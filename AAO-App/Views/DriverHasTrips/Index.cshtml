@model IEnumerable<AAO_App.Models.DriverHasTrip>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Drivers)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TripId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RequestStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Drivers.DriverId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TripId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequestStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DriverHasTripId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DriverHasTripId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DriverHasTripId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
