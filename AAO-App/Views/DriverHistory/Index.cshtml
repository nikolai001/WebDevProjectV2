@model IEnumerable<AAO_App.Models.Trip>

@{
    Layout = "_Master";
    ViewData["Title"] = "DriverHistory";
}



<div class="container">
    <h1>Mine kørte ture</h1>

    @foreach (var item in Model)
    {
        <div class="item">
            <div class="route">Afg.  @item.Cities.CityName - @item.DateStart.ToString("d'/'M") kl.@item.DateStart.ToString("HH:mm") til @item.DateEnd.ToString("d'/'M") kl.@item.DateStart.ToString("HH:mm")  </div>
            <div class="code">@item.Cities.Countries.CountryCode - @item.Cities.Countries.CountryCode</div>
            <div class="time">@Html.DisplayFor(modelItem => (item.DateEnd - item.DateStart).TotalHours) t</div>
        </div>
    }
</div>